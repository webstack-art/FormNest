{
  "name": "online-form-builder",
  "version": "1.0.0",
  "description": "An online form builder application similar to Google Forms",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run build-client",
    "build-client": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install"
  },
  "keywords": ["forms", "survey", "builder", "drag-and-drop"],
  "author": "Form Builder Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}